# The Ceyteq backend uses only the Python standard library.
# This file exists so hosts that insist on a build step are happy.
#   (make_flyers.py is the only tool that needs extra packages: pip install Pillow)
